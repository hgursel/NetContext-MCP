{
  "name": "@netcontext/network-mcp",
  "version": "0.1.0",
  "description": "MCP server for NetContext network device execution",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "netcontext-network-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "clean": "rm -rf dist tsconfig.tsbuildinfo coverage",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "network-automation",
    "ssh",
    "network-device"
  ],
  "author": "NetContext MCP Contributors",
  "license": "GPL-3.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ssh2": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/ssh2": "^1.15.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist"
  ]
}
